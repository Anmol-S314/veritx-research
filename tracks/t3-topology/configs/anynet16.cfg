topology = anynet;

network_file = traffic_matrix.txt;

routing_function = min_anynet;

traffic = uniform;
injection_rate = 0.1;

num_vcs = 4;
vc_buf_size = 8;
packet_size = 5;

sim_type = latency;
sample_period = 1000;
warmup_periods = 3;
