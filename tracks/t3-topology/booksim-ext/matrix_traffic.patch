diff --git a/src/traffic.cpp b/src/traffic.cpp
index 696d3d6..deca07a 100644
--- a/src/traffic.cpp
+++ b/src/traffic.cpp
@@ -30,6 +30,7 @@
 #include <ctime>
 #include "random_utils.hpp"
 #include "traffic.hpp"
+#include "matrixtraffic.hpp"
 
 TrafficPattern::TrafficPattern(int nodes)
 : _nodes(nodes)
@@ -91,6 +92,12 @@ TrafficPattern * TrafficPattern::New(string const & pattern, int nodes,
       perm_seed = atoi(params[0].c_str());
     }
     result = new RandomPermutationTrafficPattern(nodes, perm_seed);
+  } else if(pattern_name == "matrix") {
+    if(params.empty()) {
+      cout << "Error: matrix traffic pattern requires a filename: matrix(<file>)" << endl;
+      exit(-1);
+    }
+    result = new MatrixTrafficPattern(nodes, params[0]);
   } else if(pattern_name == "uniform") {
     result = new UniformRandomTrafficPattern(nodes);
   } else if(pattern_name == "background") {
