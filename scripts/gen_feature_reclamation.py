#!/usr/bin/env python3
"""Generate docs/product/feature-reclamation-registry.yaml.

The audit rows live here as data and are emitted with yaml.safe_dump, so
the machine-readable registry cannot be malformed by hand-editing prose.
Run:  python3 scripts/gen_feature_reclamation.py [--check]
"""
from __future__ import annotations
import sys
from pathlib import Path
import yaml

REPO = Path(__file__).resolve().parent.parent
OUT = REPO / "docs" / "product" / "feature-reclamation-registry.yaml"

HEADER = """\
# feature-reclamation-registry.yaml  (GENERATED — do not hand-edit prose)
#
# Regenerate: python3 scripts/gen_feature_reclamation.py
#
# WHAT THIS FILE IS NOT
#
#   It is NOT `capability-registry.yaml` and does not replace it. They answer
#   different questions:
#
#     capability-registry.yaml      what the CURRENT canonical product can
#                                   truthfully do today (Gate 4)
#     feature-reclamation-registry  what historically EXISTED anywhere in
#                                   VERITX/SROTA history, and what must be
#                                   reclaimed, canonicalized, rejected or
#                                   rebuilt
#
#   A `NOT_AVAILABLE` row in the capability registry does NOT prove the
#   capability should be absent. Every current NOT_AVAILABLE /
#   FUTURE_CONTRACT / ENGINE_ONLY / omitted capability was searched in
#   history before being classified here.
#
# HOW THIS WAS PRODUCED
#
#   Sources: the current integration branch/worktree; the existing migration
#   ledger `veritx_dse/application/inventory.py` (which already classifies
#   PATHS as AUTHORITATIVE / MIGRATE / LEGACY_INTERNAL / TEST_ONLY / DELETE);
#   the Gate 1-8 planning corpus; 84 branches searched with git ls-tree and
#   git grep. Capability was located in CODE AND TESTS, never inferred from
#   branch names. `third_party/` is kept separate from SROTA capability: a
#   BookSim network backend existing is NOT canonical product exposure.
#
# COVERAGE: PARTIAL — see `coverage:`. Domains under `not_yet_audited` have
# NOT been searched in history and are NOT classified.
"""

CAPS = [
 dict(id="FRA-TOPO-001", capability="broad topology family representation",
  domain="topology families",
  historical_source="BookSim network backends + t3 BookSim configs",
  exact_files=["third_party/booksim2/src/networks/ (flatfly_onchip, dragonfly, qtree, tree4, fattree, anynet, mesh, torus, ...)",
   "tracks/t3-topology/configs/{anynet16,cmesh16,dragonfly16,fattree16,flatfly16,fly4,ftree,mesh4x4,qtree16,torus4x4,tree4}.cfg"],
  tests="third_party BookSim self-tests; no SROTA canonical test",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="TopologyFamily = MESH | TORUS | CONCENTRATED_MESH | GEC | FAT_TREE (compile_model.py:597) — 5 declarable values. MaterializedFamily = MESH | TORUS | RING | CONCENTRATED_MESH (topology_artifact.py:55) — 4 materializable values. GEC and FAT_TREE are declarable but NOT materializable; RING is materializable but NOT declarable.",
  current_product_wiring="FAB-001/FAB-002/FAB-003/FAB-004",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Decide which historical families SROTA claims. The current tree's own two enumerations disagree (declarable-but-not-materializable GEC/FAT_TREE; materializable-but-not-declarable RING); that internal inconsistency must be resolved before any family is added.",
  notes="BookSim breadth is NOT canonical exposure."),

 dict(id="FRA-TOPO-002", capability="custom / arbitrary topology representation",
  domain="custom/arbitrary topology",
  historical_source="AnyNet graphs; MILP-generated .anynet output",
  exact_files=["tracks/t3-topology/configs/anynet16.cfg",
   "third_party/booksim2/src/networks/anynet.cpp",
   "veritx_dse/synthesis/milp_topology_v2.py (emits <out>.anynet)",
   "veritx_dse/tools/deadlock_routing.py (consumes .anynet)"],
  tests="no canonical declaration test",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT as intent. CAPABILITY-MATRIX.md §18 records AnyNet as exactly two compiler-owned capabilities — ROUTE-002 canonical route algorithm and ROUTE-003 backend projection — and states: 'Neither is user intent. There is no AnyNet checkbox.' FAB-007 'custom / hierarchical topology' is NOT_AVAILABLE / NO_CONTRACT.",
  current_product_wiring="FAB-007 NOT_AVAILABLE",
  classification="NEEDS_CANONICALIZATION", priority="P0",
  required_action="The routing and projection halves already exist and are canonical; only the DECLARATION half is missing. Give it a contract (FAB-007's NO_CONTRACT is the gap, not the machinery).",
  notes="The case this audit was commissioned to find: the corpus is coherent while incomplete. Planning did not reject arbitrary topology; it recorded that the two halves exist and that there is no user intent for it."),

 dict(id="FRA-SYNTH-001", capability="traffic-weighted topology synthesis (MILP/TMCF)",
  domain="topology synthesis",
  historical_source="NetSmith-method synthesizer",
  exact_files=["veritx_dse/synthesis/milp_topology_v2.py (solve_tmcf, sa_synthesize, priced_geodesic, interposer_xy, set_costs, is_bridge, base_mesh, valid_links)",
   "tracks/t3-topology/scripts/archive/milp_topology.py",
   "tracks/t3-topology/scripts/archive/milp_exactness.py",
   "tracks/t3-topology/scripts/archive/milp_exactness_norm.py"],
  tests="none dedicated (test_topology_artifact.py is unrelated)",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT",
  current_product_wiring="none",
  classification="NEEDS_CANONICALIZATION", priority="P0",
  required_action="Reclaim as a canonical synthesis capability or record a dated refusal. It generates the topology GRAPH from a traffic matrix under radix, link-length and diameter budgets with a physical layout model (grid/interposer + jitter) and wire/pipe cost pricing. Planning deferred 'Bayes/MILP' as SEARCH METHODS over a fixed space (INTENT-DESIGN-SPACE.md:512) and separately acknowledged 'a future compiler that synthesises topology from requirements is a new compiler-guidance contract and is not assumed' (INTENT-REQUIREMENTS.md:384) — the same return condition stated twice.",
  notes="inventory.py already classifies veritx_dse/synthesis/ as LEGACY_INTERNAL with replacement 'none in Wave C (synthesis science is Wave E)'. Wave E became the performance model, so the deferral landed nowhere. Documented deferral, NOT accidental erasure."),

 dict(id="FRA-SYNTH-002", capability="Bayesian-optimization topology search",
  domain="topology synthesis",
  historical_source="bo_synthesizer.py",
  exact_files=["veritx_dse/synthesis/bo_synthesizer.py",
   "veritx_dse/cli/cli.py (cmd_synthesize_bo)"],
  tests="none dedicated", historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT",
  current_product_wiring="legacy CLI only (PF-D15 marks REMOVE)",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Same disposition as FRA-SYNTH-001."),

 dict(id="FRA-SYNTH-003", capability="iterative (RHO/GRPO) topology search",
  domain="topology synthesis",
  historical_source="iterative_synthesizer.py",
  exact_files=["veritx_dse/synthesis/iterative_synthesizer.py",
   "veritx_dse/cli/cli.py (cmd_synthesize_iterative)"],
  tests="none dedicated", historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT",
  current_product_wiring="legacy CLI only",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Same disposition as FRA-SYNTH-001."),

 dict(id="FRA-SYNTH-004", capability="event-driven topology objective scoring",
  domain="topology synthesis",
  historical_source="event_objective.py — scores a candidate topology from the event timeline",
  exact_files=["veritx_dse/synthesis/event_objective.py",
   "tracks/t3-topology/dse/scripts/event_objective.py"],
  tests="none dedicated", historical_maturity="EXPERIMENTAL",
  current_canonical_representation="ABSENT", current_product_wiring="none",
  classification="RESEARCH_ONLY", priority="P2",
  required_action="Keep as research. It couples topology scoring to a temporal model (Wave E), so it cannot be reclaimed before the temporal layer is canonical."),

 dict(id="FRA-SYNTH-005", capability="topology physical constraints (radix, link length, diameter, layout, wire/pipe pricing)",
  domain="topology physical constraints",
  historical_source="milp_topology_v2 layout + cost model",
  exact_files=["veritx_dse/synthesis/milp_topology_v2.py (grid_xy, interposer_xy, valid_links, set_costs, _edge_len, priced_geodesic)"],
  tests="none dedicated", historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT (concentration exists; no link-length or diameter budget)",
  current_product_wiring="none",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="These constraints are what make a generated topology physically meaningful. Decide whether they belong in canonical intent or in a synthesis-only contract; do not silently drop them.",
  notes="PIPE_COST / WIRE_COST are module constants reused by event_objective.py, so the pricing model has at least one live consumer outside the synthesizer."),

 dict(id="FRA-OPT-001", capability="topology as an optimization variable",
  domain="topology as optimization dimension",
  historical_source="synthesis MILP/SA emits a topology as its OUTPUT",
  exact_files=["veritx_dse/synthesis/milp_topology_v2.py"],
  tests="none", historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT. The canonical design space enumerates topology_family over a finite set (INTENT-DESIGN-SPACE.md); it never GENERATES a graph. OPT-002 'expert typed design space' is WIRED but bounded by DomainParam finite value tuples.",
  current_product_wiring="none",
  classification="NEEDS_CANONICALIZATION", priority="P0",
  required_action="Decide whether topology generation is in the product thesis. If it is, it needs a canonical candidate representation that is NOT a DomainParam (a generated graph has no finite value tuple) plus a candidate-identity rule.",
  notes="Distinct from FRA-SYNTH-001: that is the generator, this is whether a GENERATED topology can be a study candidate at all."),

 dict(id="FRA-TOPO-003", capability="multiplane fabric",
  domain="multiplane",
  historical_source="t3 BookSim multiplane configs",
  exact_files=["tracks/t3-topology/configs/plane_control.cfg",
   "tracks/t3-topology/configs/plane_data.cfg",
   "tracks/t3-topology/configs/plane_shared.cfg",
   "tracks/t3-topology/configs/plane_cmesh.cfg",
   "tracks/t3-topology/configs/plane_cmesh_ctrl.cfg"],
  tests="none", historical_maturity="EXPERIMENTAL",
  current_canonical_representation="ABSENT",
  current_product_wiring="FAB-005 multiplane NOT_AVAILABLE / FUTURE_CONTRACT",
  classification="RESEARCH_ONLY", priority="P2",
  required_action="Keep as research until a canonical multiplane contract exists. The configs are BookSim experiments, not a canonical derivation, and no code produces them.",
  notes="The word 'multiplane' appears in NO SROTA source file. The evidence is five BookSim config names only, which is why maturity is EXPERIMENTAL and not IMPLEMENTED."),

 dict(id="FRA-ROUTE-001", capability="dimension-order routing (DOR / DOR-XY)",
  domain="routing algorithms",
  historical_source="canonical compiler",
  exact_files=["veritx_dse/model/routing.py (derive_route, _CERTIFIED_FAMILIES)",
   "veritx_dse/model/routing_materialize.py (DOR_XY family)"],
  tests="test_routing_realization.py, test_route_artifact.py",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL — RouteArtifact with RoutingClassDefinition(id='DOR_XY')",
  current_product_wiring="ROUTE-001 WIRED",
  classification="CANONICAL_NOW", priority="P3", required_action="none"),

 dict(id="FRA-ROUTE-002", capability="BookSim AnyNet Dijkstra routing",
  domain="routing algorithms",
  historical_source="BookSim backend",
  exact_files=["third_party/booksim2/src/networks/anynet.cpp",
   "veritx_dse/core/route_artifact.py (_anynet_replica_first_hops replicates AnyNet::route() tie-break semantics exactly)"],
  tests="test_route_artifact.py", historical_maturity="SEALED",
  current_canonical_representation="ROUTE-003 (AnyNet backend projection); COND-CANONICAL-ROUTE-PRESENT",
  current_product_wiring="ROUTE-003; CAP-ENV-BOOKSIM-ANYNET-V1",
  classification="CANONICAL_NOW", priority="P3", required_action="none"),

 dict(id="FRA-ROUTE-003", capability="adaptive routing (MinAdapt / UGAL / congestion-aware)",
  domain="routing algorithms",
  historical_source="routing_policy vocabulary; BookSim adaptive backends",
  exact_files=["veritx_dse/model/routing_policy.py — a full RoutingPolicyDefinition artifact (PathMode, DecisionScope, CandidateMode, SelectionLocus, RandomnessMode, RoutingStateKind, RuntimeObservation, RoutingResourceRoleKind incl. ADAPTIVE, DeadlockProofObligation), content-addressed",
   "veritx_dse/model/router_behavior.py — 'Explicitly absent: escape/adaptive priority, routing-action priority, congestion thresholds, MinAdapt/UGAL arbitration, hidden QoS priority'"],
  tests="test_routing_policy.py (depth not audited)",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="PARTIAL. The routing-policy VOCABULARY exists and models adaptive routing; the canonical compile path derives DOR_XY only. The router-behavior artifact excludes MinAdapt/UGAL BY DESIGN.",
  current_product_wiring="ROUTE-009 adaptive NOT_AVAILABLE / FUTURE_CONTRACT",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Resolve the internal tension: a canonical adaptive-policy vocabulary exists while the behavior artifact states adaptive arbitration is absent. Decide which is authoritative and make the registry row match.",
  notes="Do NOT classify REJECTED_UNSOUND: the router_behavior exclusion is a stated scope boundary of that artifact, not a claim that adaptive routing is unsound."),

 dict(id="FRA-ROUTE-004", capability="min-max-load routing (MCLB / ILP)",
  domain="routing optimization",
  historical_source="CLI + deadlock tooling",
  exact_files=["veritx_dse/cli/cli.py", "veritx_dse/tools/deadlock_routing.py"],
  tests="none canonical", historical_maturity="EXPERIMENTAL",
  current_canonical_representation="ABSENT", current_product_wiring="none",
  classification="RESEARCH_ONLY", priority="P2",
  required_action="Audit depth not reached in this pass. It appears in a tool and a CLI command, not in a canonical derivation. Confirm before any promotion."),

 dict(id="FRA-ROUTE-005", capability="valiant / randomized routing",
  domain="routing algorithms",
  historical_source="unknown",
  exact_files=["veritx_dse/model/packet_format.py (single mention)"],
  tests="none", historical_maturity="CONCEPT_ONLY",
  current_canonical_representation="ABSENT",
  current_product_wiring="ROUTE-010 valiant NOT_AVAILABLE / FUTURE_CONTRACT",
  classification="RESEARCH_ONLY", priority="P3",
  required_action="One mention in packet_format.py is not an implementation. Do not promote on the strength of a keyword hit."),

 dict(id="FRA-ROUTE-006", capability="escape VC designation",
  domain="VC assignment",
  historical_source="canonical compiler",
  exact_files=["veritx_dse/model/vc_assignment.py", "veritx_dse/compiler/candidate_policy.py",
   "veritx_dse/backend/meshdor.py", "veritx_dse/backend/booksim.py"],
  tests="test_channel_vc_cdg.py, test_adaptive_escape.py",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL — VCAssignmentArtifact carries escape_vcs",
  current_product_wiring="ROUTE-008 NOT_AVAILABLE / IMPLEMENTATION_GAP",
  classification="NEEDS_CANONICALIZATION", priority="P2",
  required_action="The artifact carries escape_vcs and the code is widespread, yet ROUTE-008 says IMPLEMENTATION_GAP. Determine whether the gap is product exposure or a real derivation gap, and correct whichever row is wrong."),

 dict(id="FRA-ROUTE-007", capability="RCU / in-network reduction",
  domain="RCU / in-network reduction",
  historical_source="none found", exact_files=[], tests="none",
  historical_maturity="ABSENT", current_canonical_representation="ABSENT",
  current_product_wiring="ROUTE-011 FUTURE_CONTRACT (removed from v4)",
  classification="REJECTED_UNSOUND", priority="P3",
  required_action="Confirmed absent in history: `rcu`/`RCU` matches ZERO SROTA source files on any branch. The Gate-5 removal was correct — there was never an implementation to remove, only an intent field.",
  notes="The cleanest REJECTED_UNSOUND in the audit: planning removed a control for a capability that never existed."),

 dict(id="FRA-WORK-001", capability="full workload operation vocabulary",
  domain="workload graph operation vocabulary",
  historical_source="Wave-D canonical workload graph",
  exact_files=["veritx_dse/workload/graph.py (KIND_COMPUTE, KIND_COLLECTIVE, KIND_P2P, KIND_MULTICAST, KIND_EXPERT_BEGIN, KIND_EXPERT_END, KIND_PIM_CHANNEL, KIND_PIM_END)",
   "veritx_dse/workload/canonical_graph.py",
   "veritx_dse/workload/{canonical,lowering,messages,migration,timeline}.py",
   "veritx_dse/backend/astra.py"],
  tests="workload tests present (depth not audited)",
  historical_maturity="SEALED",
  current_canonical_representation="TWO LAYERS, not one. Evaluation side: the 8-kind Wave-D graph, reachable via application/requests.py and fabric_evaluator.py. Design-intent side: WorkloadV3 carries `collectives` with only CollectiveKind = allreduce | allgather | reducescatter | broadcast | alltoall, plus DependencyGraph. So P2P, MULTICAST, EXPERT_DISPATCH/COMBINE and PIM are expressible downstream but NOT declarable as canonical Design intent.",
  current_product_wiring="WORK-001/WORK-002/WORK-004",
  classification="NEEDS_CANONICALIZATION", priority="P0",
  required_action="INTENT-WORKLOAD.md:122-129 reconciled the PHASE-9 grammar against Wave-D (P2P RENAMED_EQUIVALENT, MULTICAST EXACT_EQUIVALENT, EXPERT_BEGIN RENAMED_EQUIVALENT, PIM_CHANNEL/PIM_END PARTIAL_EQUIVALENT -> MEMORY). That reconciliation does NOT cover the WorkloadV3 -> Wave-D gap. Close it explicitly: either extend WorkloadV3 or record that Design intent is deliberately collective-only.",
  notes="Planning reconciled the OLD grammar; the audit found the CURRENT intent vocabulary is narrower than the canonical graph it must lower to. A new finding, not a restatement."),

 dict(id="FRA-WORK-002", capability="PIM (processing-in-memory) channel semantics",
  domain="PIM",
  historical_source="Wave-D + ASTRA",
  exact_files=["veritx_dse/workload/graph.py (KIND_PIM_CHANNEL, KIND_PIM_END, _PIM_CHANNEL_KEYS = {'channel'})",
   "veritx_dse/backend/astra.py"],
  tests="present (depth not audited)", historical_maturity="IMPLEMENTED",
  current_canonical_representation="PARTIAL. Present in the Wave-D graph and the ASTRA backend; absent from WorkloadV3 (Design intent). Planning records PIM_CHANNEL as PARTIAL_EQUIVALENT -> MEMORY (INTENT-WORKLOAD.md:128).",
  current_product_wiring="MEM-007 PIM NOT_AVAILABLE / LEGACY_ONLY",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="LEGACY_ONLY understates it: PIM_CHANNEL is live in the Wave-D graph and the ASTRA backend, not merely a legacy grammar token. Reclassify or justify.",
  notes="Direct instance of the audit's critical rule — a NOT_AVAILABLE registry row whose historical evidence is stronger than the row implies."),

 dict(id="FRA-PERF-001", capability="Wave-E temporal / system-performance model",
  domain="Wave-E temporal/system-performance model",
  historical_source="Wave-E sealed performance layer",
  exact_files=["veritx_dse/performance/model.py (PerformanceModel, ClockDef, ResourceDef, fidelity_warning, rate_duration)",
   "veritx_dse/performance/workload.py (PerformanceRequest, TemporalEvent, TemporalWorkload, canonical_events)",
   "veritx_dse/performance/scheduler.py (schedule_workload, Schedule, ScheduledEvent, SchedulerDeadlock)",
   "veritx_dse/performance/result.py (PerformanceEventGraph, build_performance_result, reverify_result)",
   "veritx_dse/performance/network.py (NetworkWindowBinding, bind_network_window, stats_sha256, network_window_duration)",
   "veritx_dse/performance/metrics.py (dependency_critical_path, resource_utilization, request_latencies, latency_summary)",
   "veritx_dse/performance/sensitivity.py (perturb_model, perturb_workload_durations, sensitivity_analysis)",
   "veritx_dse/application/wave_e_resources.py"],
  tests="test_wave_e_identity.py, test_wave_e_scheduler.py, test_wave_e_product.py, test_wave_e_demos.py, test_serving_scheduling_oracle.py",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL and WIRED. application/fabric_evaluator.py builds PerformanceModel + TemporalWorkload, runs schedule_workload -> build_performance_result -> reverify_result, and binds the BookSim window.",
  current_product_wiring="EVAL-001..EVAL-008; application/wave_e_resources.py",
  classification="CANONICAL_NOW", priority="P3", required_action="none"),

 dict(id="FRA-PERF-002", capability="Wave-E metric exposure (makespan, critical path, utilization, request latency, TTFT, decode-step latency, sensitivity)",
  domain="Wave-E temporal/system-performance model",
  historical_source="Wave-E declared metric set",
  exact_files=["veritx_dse/application/capabilities.py (wave_e.metrics)",
   "veritx_dse/performance/metrics.py",
   "veritx_dse/performance/sensitivity.py"],
  tests="test_wave_e_demos.py, test_wave_e_product.py",
  historical_maturity="SEALED",
  current_canonical_representation="COMPUTED but only PARTLY product-exposed. The product surfaces completion_cycles / completion_ns / completion_time. Wave-E additionally computes makespan, critical_path, utilization, request_latency, ttft, decode_step_latency and sensitivity.",
  current_product_wiring="metric registry (optimization/metric_registry.py) exposes the completion_* family",
  classification="RECLAIMABLE", priority="P0",
  required_action="REGISTER the metrics. All four RECLAIMABLE conditions hold: the semantics are sealed Wave-E facts; the implementation exists (performance/metrics.py, performance/sensitivity.py); the canonical representation ALREADY FITS because CertifiedMetricRegistry exposes MetricRegistryBuilder.register(metric, producer, ...) and is already wired into real_evaluator.py and result.py; only the registration is missing. This is the one genuine RECLAIMABLE in the audit.",
  notes="Wave-E is NOT a reclamation item (it is sealed and wired). The reclamation item is its product projection."),

 dict(id="FRA-PERF-003", capability="Wave-E honesty boundary (unsupported list + fidelity warning + predictive validation status)",
  domain="Wave-E temporal/system-performance model",
  historical_source="Wave-E capability declaration",
  exact_files=["veritx_dse/application/capabilities.py (wave_e.unsupported, wave_e.predictive_validation, compute.calibration)",
   "veritx_dse/performance/model.py (fidelity_warning)"],
  tests="test_wave_e_product.py",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL. Explicitly unsupported: throughput, continuous_batching, per_operation_network_causality, analytical_compute_roofline, memory_capacity. compute ANALYTICAL_MODEL is MEMORY_ONLY and UNCALIBRATED. predictive_validation NOT_ESTABLISHED.",
  current_product_wiring="application/capabilities.py",
  classification="CANONICAL_NOW", priority="P0",
  required_action="Preserve verbatim when Phase 3 projects metrics. These declarations are what stop an analytical estimate being read as a measured claim."),

 dict(id="FRA-PHYS-001", capability="analytical area / power / timing estimates",
  domain="physical/reporting (area, power, leakage, Fmax, energy)",
  historical_source="PRD §7 formal estimation models",
  exact_files=["veritx_dse/reports/reports.py (estimate_router_area, estimate_link_area, estimate_nic_area, estimate_fabric_area, estimate_dynamic_power, estimate_leakage_power, estimate_total_power, compute_energy_per_bit, router_pipeline_stages, estimate_critical_path_ps, estimate_max_frequency, generate_report, _scale_factor)"],
  tests="test_reports.py",
  historical_maturity="TESTED",
  current_canonical_representation="PRESENT and TESTED but NOT product-wired. Exported from veritx_dse/__init__.py; imported by no application/product module. NOT listed in application/inventory.py.",
  current_product_wiring="none",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Classify honestly as ANALYTICAL_ESTIMATE and decide the home: report capability, optimization objective, or comparison metric. Do NOT promote to signoff. The model cites DAC 2022 router area, ARM CMN-600 and JEDEC HBM3 and scales as (process_nm/7)^2; it is an estimate by construction.",
  notes="Fidelity ladder for this row: ANALYTICAL_ESTIMATE (not TOOL_DERIVED, not MEASURED, not SIGNOFF). Its absence from inventory.py is itself a finding."),

 dict(id="FRA-PHYS-002", capability="HMAC-signed design manifests / export bundles",
  domain="signed manifests/export bundles",
  historical_source="PRD §12/§14 artifact signing",
  exact_files=["veritx_dse/reports/artifact.py (sign_manifest, verify_manifest, DesignManifest, _DEFAULT_SECRET)"],
  tests="test_artifact_primitives.py, test_build_manifest.py",
  historical_maturity="TESTED",
  current_canonical_representation="PRESENT but not product-wired. Self-declared: 'The signing is NOT a full PKI system - it uses a shared secret key'. PRD full cryptographic signing (§14.4) is deferred.",
  current_product_wiring="none",
  classification="NEEDS_CANONICALIZATION", priority="P2",
  required_action="Decide whether this is product provenance or developer tooling. Integrity/provenance via a shared secret is real but must never be presented as PKI signing or as third-party verifiable."),

 dict(id="FRA-BACK-001", capability="Ramulator DRAM/HBM engine",
  domain="Ramulator full depth",
  historical_source="Ramulator 2.1 integration",
  exact_files=["veritx_dse/simulation/ramulator.py (RamulatorBackend, MemoryEvidence, execute, _verdict, _check_supported, manifest_hash, _verify_chain, _metric, discover)",
   "veritx_dse/model/memory_lowering*"],
  tests="test_ramulator_backend.py, test_ramulator_lowering.py",
  historical_maturity="TESTED",
  current_canonical_representation="PRESENT and typed, with a lowering manifest, a supported-configuration check and an evidence chain. Registry: MEM-002 ENGINE_ONLY under CAP-ENV-RAMULATOR-V1, drain contract issued == accepted == served.",
  current_product_wiring="MEM-002 ENGINE_ONLY; MEM-003/MEM-004 NOT_AVAILABLE",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="ENGINE_ONLY appears ACCURATE for the engine itself (it runs and produces evidence) but the audit did not verify the full scheduler/row-policy/clock/capacity/conservation surface, nor the PASS/FAILED/INCONCLUSIVE/UNSUPPORTED vocabulary end to end. Verify before treating MEM-003/MEM-004 as absent rather than un-wired.",
  notes="Depth NOT completed in this pass: exact DRAM/HBM config set, row/scheduler policy, clock assumptions, capacity checks, transaction conservation."),

 dict(id="FRA-BACK-002", capability="ASTRA-Sim execution, namespace and machine binding",
  domain="ASTRA runtime diagnostics",
  historical_source="ASTRA-Sim integration",
  exact_files=["veritx_dse/backend/astra.py", "veritx_dse/backend/astra_execution.py",
   "veritx_dse/backend/astra_machine.py", "veritx_dse/backend/astra_namespace.py"],
  tests="test_astra_projection.py, test_astra_runtime.py, test_astra_timing_oracle.py, test_astra_trace.py, test_backend_astra_machine.py, test_backend_astra_namespace.py",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL. Includes an independent closed-form timing oracle (R2) that replaced the two-binary differential as the release-critical check.",
  current_product_wiring="serving path (CERTIFIED_SERVING_BOOKSIM2_V1)",
  classification="CANONICAL_NOW", priority="P3", required_action="none"),

 dict(id="FRA-BACK-003", capability="MTU / fragmentation boundary",
  domain="MTU/fragmentation",
  historical_source="Wave-D packet delimitation",
  exact_files=["veritx_dse/backend/astra.py (# MTU / fragmentation boundary)",
   "veritx_dse/backend/booksim.py (embedded_mtu 'not proven/deferred to Wave D')",
   "veritx_dse/backend/meshdor.py"],
  tests="not isolated in this pass",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="DECLARED AS NOT ACTIVATED: BookSim records 'fragmentation mechanism is not activated or proven' and 'embedded_mtu not proven/deferred to Wave D'. ASTRA carries the boundary.",
  current_product_wiring="none",
  classification="RESEARCH_ONLY", priority="P2",
  required_action="Keep the explicit 'not proven' declaration. Do not let a future claim imply fragmentation is modeled."),

 dict(id="FRA-SERVE-001", capability="serving runtime, protocol and trace lowering",
  domain="serving depth / LLMServingSim scheduler",
  historical_source="LLMServingSim + canonical serving loop",
  exact_files=["veritx_dse/simulation/serve_canonical.py", "veritx_dse/simulation/serving_loop.py",
   "veritx_dse/simulation/serving_runtime.py", "veritx_dse/simulation/serving_dp.py",
   "veritx_dse/simulation/serving_liveness.py", "veritx_dse/simulation/llmserving_protocol.py",
   "veritx_dse/simulation/model_to_trace.py", "veritx_dse/simulation/trace_to_binary.py",
   "veritx_dse/simulation/traces.py", "veritx_dse/backend/canonical_serving.py"],
  tests="12 serving test modules incl. test_serving_dp.py, test_serving_ep.py, test_serving_tp_groups.py, test_serving_multiinstance_liveness.py, test_serving_liveness.py, test_serving_round.py, test_serving_canonical.py, test_serving_scheduling_oracle.py",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL and WIRED. TP groups, DP, EP, multi-instance liveness, request traces and a scheduling oracle all have dedicated tests.",
  current_product_wiring="WORK-003/WORK-004; CERTIFIED_SERVING_BOOKSIM2_V1; /projects/{id}/serving",
  classification="CANONICAL_NOW", priority="P3", required_action="none",
  notes="Depth NOT completed: arrivals, batching, prefill/decode split, MoE dispatch/combine detail, memory tiers, profiling, analytical vs BookSim backend comparison. The module and test surface is large enough that a shallow claim would be unsafe."),

 dict(id="FRA-RTL-001", capability="RTL generation + Verilator build/run + RTL authority",
  domain="RTL / Verilator",
  historical_source="T3 RTL and validation harness",
  exact_files=["validation/harness/rtl.py (RtlPacket, RtlResult, mesh_radix, write_traces, build [invokes 'verilator --binary -j 4 --timing'], run, run_authority)",
   "tracks/t3-topology/rtl/t3", "tracks/t3-topology/rtl/mot_htree",
   "tracks/t3-topology/rtl/cdc", "tracks/t3-topology/rtl/mot_htree/gen_rtl_htree.py"],
  tests="validation/tests/ (7 modules) and validation/experiments (14)",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="PRESENT. Verilator is genuinely EXECUTED (build + run + run_authority), not merely referenced. RTL directories exist for t3, mot_htree and cdc.",
  current_product_wiring="none - validation harness is a release/validation surface, not the product path",
  classification="RESEARCH_ONLY", priority="P2",
  required_action="Keep as a validation authority. Do NOT describe this as formal verification: it is simulation of generated RTL against an oracle.",
  notes="Distinction the brief requires: generator exists YES; simulation exists YES; qualified parity exists (validation/harness/compare.py standalone_parity with authority_class standalone_booksim); current canonical product integration NO."),

 dict(id="FRA-RTL-002", capability="UVM testbench / sequence / assertion / coverage generation",
  domain="UVM generation, SVA/assertion generation",
  historical_source="PRD §12 collateral generation",
  exact_files=["veritx_dse/verification/uvm_gen.py (generate_uvm, _gen_tb_top, _gen_sequences, _gen_assertions, _gen_coverage, sequences: vc_separation_seq, injected_seq, random_seq, alltoall_seq, hotspot_seq, noc_coverage)"],
  tests="test_uvm_gen.py, test_compile_uvm.py",
  historical_maturity="TESTED",
  current_canonical_representation="GENERATION CAPABILITY. Emits testbench top, sequences, assertion families and a coverage component.",
  current_product_wiring="CLI 'generate uvm' (legacy namespace)",
  classification="RESEARCH_ONLY", priority="P2",
  required_action="Preserve as GENERATION capability only. Nothing in this repository executes or proves the emitted assertions, so it must never be described as formal verification or as proven coverage."),

 dict(id="FRA-VALID-001", capability="validation harness (authority, engines, oracle, metamorphic, mutations, intervention, corpus)",
  domain="backend qualification, evidence/provenance, fault injection",
  historical_source="B4 validation campaign",
  exact_files=["validation/harness/authority.py", "validation/harness/engines.py",
   "validation/harness/oracle.py", "validation/harness/compare.py",
   "validation/harness/metamorphic.py", "validation/harness/mutations.py",
   "validation/harness/intervention.py", "validation/harness/fabric.py",
   "validation/corpus.py", "validation/FINDINGS.md"],
  tests="validation/tests/ (7 modules incl. test_mutations.py, test_metamorphic.py, test_intervention.py, test_engines.py, test_corpus.py, test_collective_graph.py, test_production_corpus.py)",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL. 24 machine-readable reports are projected by application/product/validation.py into the Trust surface; prose campaigns are linked, never parsed.",
  current_product_wiring="GET /api/v1/validation; Trust page",
  classification="CANONICAL_NOW", priority="P3",
  required_action="Fault injection is mutation-based (validation/harness/mutations.py) rather than a runtime fault injector; keep that distinction explicit."),

 dict(id="FRA-CMP-001", capability="comparison contract (controlled/intentional variables, fidelity, metric and payload compatibility)",
  domain="comparison/comparability",
  historical_source="ComparisonIntent / evaluate_comparability",
  exact_files=["veritx_dse/core/comparison.py (ComparisonIntent, ComparisonSpecError, eval_metric_compatibility, FINGERPRINT_FIELDS, REQUIRED_BY_FIDELITY, fingerprint_from_run, fingerprint_from_legacy_row, ComparisonVerdict, evaluate_comparability, PARETO_STATUSES)"],
  tests="test_optimization_identity_exact.py and comparison tests",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL. Includes KNOWN_UNITS for metric compatibility and REQUIRED_BY_FIDELITY for fidelity-gated comparison.",
  current_product_wiring="GET /api/v1/compare; Compare page",
  classification="CANONICAL_NOW", priority="P3",
  required_action="Depth NOT completed: percentage-delta admissibility, network-clock compatibility and the full incomparable-outcome set were not traced against the planned Compare flow."),

 dict(id="FRA-WF-001", capability="Wave-F optimization definition/result identity and search",
  domain="Wave-F design optimization",
  historical_source="Wave-F sealed optimizer",
  exact_files=["veritx_dse/optimization/definition.py", "veritx_dse/optimization/result.py",
   "veritx_dse/optimization/search.py", "veritx_dse/optimization/candidate.py",
   "veritx_dse/optimization/constraints.py", "veritx_dse/optimization/evaluators.py",
   "veritx_dse/optimization/metric_registry.py", "veritx_dse/optimization/pareto.py",
   "veritx_dse/optimization/real_evaluator.py", "veritx_dse/optimization/CAPABILITY-LEDGER.md"],
  tests="test_optimization_definition_identity.py, test_optimization_identity_exact.py, test_p2_guided_optimization.py, test_p2_optimization_truth.py, test_p1_optimize_booksim.py, test_candidate_policy.py, test_optimize_canonical_cli.py",
  historical_maturity="SEALED",
  current_canonical_representation="CANONICAL and WIRED (OPT-001..OPT-004). An adversarial optimization-truth suite exists (test_p2_optimization_truth.py).",
  current_product_wiring="POST /api/v1/revisions/{id}/optimize; Optimize surface",
  classification="CANONICAL_NOW", priority="P3",
  required_action="Depth NOT completed: the 21-item Wave-F parity matrix (aliases, invalid candidates, multi-scenario, ties, exhaustive vs budgeted completeness, NOT_EVALUATED tails, reopen verification, transplant/tamper attacks) was not built in this pass. The RT audit branches hold the strongest evidence for it."),

 dict(id="FRA-WF-002", capability="topology as a design-space variable",
  domain="topology as optimization dimension",
  historical_source="synthesis MILP/SA emits a topology",
  exact_files=["veritx_dse/synthesis/milp_topology_v2.py", "veritx_dse/optimization/definition.py"],
  tests="none", historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT. DomainParam carries finite value tuples; a generated graph has no finite tuple, so topology generation is not expressible as a dimension.",
  current_product_wiring="OPT-002 expert typed design space",
  classification="NEEDS_CANONICALIZATION", priority="P0",
  required_action="CONCLUSION: the current optimizer is a NARROWER PARAMETER OPTIMIZER, not a topology-search optimizer. It has no topology-graph, link-budget, radix, link-length or physical-layout dimension. State that plainly rather than implying search parity.",
  notes="This is the explicit conclusion the brief demands, not an ambiguous one."),

 dict(id="FRA-STUDIO-001", capability="historical Studio surfaces",
  domain="Studio historical surfaces",
  historical_source="p4/studio and RT Studio hardening",
  exact_files=["apps/studio/src/components/{DesignEditor,EvaluateView,FabricCanvas,OptimizeView,VerifyView,badges}.tsx on origin/p4/studio",
   "current apps/studio/src/{pages,components}/"],
  tests="apps/studio/tests/test_studio_contract_v2.py (2 pre-existing failures)",
  historical_maturity="TESTED",
  current_canonical_representation="Design/Review/Compile Result/Fabric inspector rebuilt on DesignViewV2 and CompileResultView (Phase 1-2). EvaluateView, OptimizeView, VerifyView and FabricCanvas survive; DesignEditor was replaced by DesignViewV2Editor.",
  current_product_wiring="WIRED",
  classification="CANONICAL_NOW", priority="P3",
  required_action="Depth NOT completed: the historical p4/studio screen inventory was read from the branch tree, not from its source semantics, so per-surface classification (PRESERVED / REPLACED / REMOVED_CORRECTLY) is incomplete."),

 dict(id="FRA-EX-001", capability="historical example corpus as regression probes",
  domain="historical examples",
  historical_source="v3 workload templates + DSE example presets",
  exact_files=["tracks/t3-topology/examples/{dense_1b_16tiles,dense_4b_32tiles_conc4,llama_dense_64tiles,moe_8x7b_64tiles}-v3.json and llama_dense_64tiles.json",
   "tracks/t3-topology/dse/examples/{dense_64npu,llama1b_tp64,llama70b_tp64,moe_8npu,qwen3_moe_16npu}.json"],
  tests="test_integration.py and test_product_workflow.py consume examples/",
  historical_maturity="TESTED",
  current_canonical_representation="PARTIAL. Four v3 templates are product presets with certification; the DSE example presets (dense_64npu, llama1b_tp64, llama70b_tp64, moe_8npu, qwen3_moe_16npu) are legacy and are NOT canonical regressions.",
  current_product_wiring="workload catalog for the v3 set only",
  classification="NEEDS_CANONICALIZATION", priority="P2",
  required_action="Depth NOT completed: no mesh/torus/GEC/MECS/FlatFly/Dragonfly/AnyNet/allreduce/alltoall/broadcast/multicast/hotspot/chiplet/HPC/PIM/CXL example was individually mapped to a canonical regression. The BookSim configs (11 topologies) and plane_* configs are the likely probe set."),

 dict(id="FRA-MEM-001", capability="REMOTE / CXL / STORAGE memory locations",
  domain="CXL, remote memory, memory-location semantics",
  historical_source="Wave-D memory-location grammar",
  exact_files=["veritx_dse/workload/memory_lowering.py (explicit refusals: REMOTE 'remote access mislabeled, not local memory'; CXL; STORAGE 'package storage is never HBM. Refused.')",
   "veritx_dse/workload/canonical.py", "veritx_dse/workload/graph.py",
   "veritx_dse/core/memory.py", "veritx_dse/simulation/serving_loop.py"],
  tests="memory lowering tests",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="GRAMMAR TOKENS, refused at lowering with typed reasons against the v1 HBM pool. This matches MEM-006 'grammar tokens, refused at lowering against the v1 HBM pool' exactly.",
  current_product_wiring="MEM-006 NOT_AVAILABLE / LEGACY_ONLY",
  classification="REJECTED_UNSOUND", priority="P3",
  required_action="None for the v1 pool: the refusal is typed, reasoned and tested, and 'eh, use HBM' is explicitly rejected. If a remote-memory tier is ever wanted it needs a NEW contract, not a re-enable.",
  notes="LEGACY_ONLY is accurate here, unlike MEM-007 PIM where it understates live code."),

 dict(id="FRA-PHYS-003", capability="Timeloop / Accelergy NoC energy spine",
  domain="Timeloop, energy",
  historical_source="T3 analysis pipeline",
  exact_files=["tracks/t3-topology/timeloop/{arch,mapper,problem,noc_ERT,noc_arch}.yaml",
   "tracks/t3-topology/timeloop/experiment_configs/", "tracks/t3-topology/timeloop/problem_library/",
   "tracks/t3-topology/Makefile (timeloop, energy targets)"],
  tests="pipeline runs via the Makefile; no canonical test",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="PRESENT as a research pipeline. inventory.py classifies the T3 scripts pipeline LEGACY_INTERNAL, 'research-only, not product'.",
  current_product_wiring="none",
  classification="RESEARCH_ONLY", priority="P2",
  required_action="Keep as research. Timeloop/Accelergy energy is TOOL_DERIVED for compute/accelerator energy, not a NoC power signoff; it must not be merged with reports.py ANALYTICAL_ESTIMATE power.",
  notes="Fidelity: TOOL_DERIVED, and optional even in the pipeline (Makefile tolerates accelergy being absent)."),

 dict(id="FRA-WF-003", capability="Wave-F search completeness accounting (EXHAUSTIVE_GRID / BUDGETED_GRID / NOT_EVALUATED)",
  domain="Wave-F candidate persistence/reopen verification",
  historical_source="Wave-F optimization/space.py budget_plan, optimization/result.py derive_budget/derive_search_complete",
  exact_files=["veritx_dse/optimization/search.py::_budget_limit and search_candidates (budget applied as silent prefix truncation cands[:limit])",
   "veritx_dse/optimization/definition.py::OptimizationDefinition.budget",
   "veritx_dse/optimization/CAPABILITY-LEDGER.md row W11 (HISTORICAL)",
   "historical: tracks/t3-topology/docs/audit/WAVE-F-PARITY-LEDGER.md on wave-f/design-optimization"],
  tests="test_p2_guided_optimization.py",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT. The three labels existed historically (EXHAUSTIVE_GRID in 2 commits, BUDGETED_GRID in 3, NOT_EVALUATED in 4) and are gone from the tree. search_candidates truncates with cands[:limit] and emits no completeness fact, so a consumer cannot distinguish an exhaustive search from a truncated one. CAPABILITY-LEDGER W11 records the full budget-tail machinery as HISTORICAL with reason 'no ALIAS/INVALID/NOT_EVALUATED states in P2'.",
  current_product_wiring="optimization/result.py study view",
  classification="NEEDS_CANONICALIZATION", priority="P0",
  required_action="Restore completeness as a first-class result fact: an exhaustive search must be declarable and a truncated search must declare itself, with a visible un-evaluated tail. Silent truncation is the exact failure mode this vocabulary existed to prevent.",
  notes="Not accidental erasure: CAPABILITY-LEDGER.md records the deferral explicitly."),

 dict(id="FRA-WF-004", capability="Wave-F multi-scenario evaluation and cross-scenario hardware consistency",
  domain="Wave-F candidate persistence/reopen verification",
  historical_source="Wave-F scenario template and comparability gate",
  exact_files=["veritx_dse/optimization/CAPABILITY-LEDGER.md rows W5 and W9",
   "veritx_dse/optimization/pareto.py::pareto_with_sealed_gate"],
  tests="none in the current tree",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="ABSENT. grep -c scenario over optimization/*.py is ZERO. Ledger W5: 'Wave-F scenario/multi-model comparability gate SUPERSEDED - P2 has one scenario, one fake fidelity'. Ledger W9 records the comparability gate as HISTORICAL.",
  current_product_wiring="none",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Return with the real multi-backend adapter. Until then the product must state that optimization is single-scenario by construction.",
  notes="DOCUMENTED_DEFERRAL, not erasure."),

 dict(id="FRA-WF-005", capability="Wave-F verified metric extraction (W7) - deferral discharged",
  domain="Wave-F candidate persistence/reopen verification",
  historical_source="Wave-F optimization/metrics.py",
  exact_files=["veritx_dse/optimization/metric_registry.py (CertifiedMetricRegistry, MetricAuthority, MetricRegistryBuilder.register, ExperimentalMetricRegistry, extract)",
   "veritx_dse/optimization/real_evaluator.py (imports metric_registry)",
   "veritx_dse/optimization/result.py (imports CertifiedMetricRegistry, _has_metric_authority, _authoritative_metrics)"],
  tests="test_p2_real_adapter.py, test_p2_optimization_truth.py",
  historical_maturity="SEALED",
  current_canonical_representation="LANDED. CAPABILITY-LEDGER row W7 recorded 'REJECT (deferred) - no optimization/metrics.py in P2; returns with the real adapter'. metric_registry.py now exists and is wired into real_evaluator.py and result.py, with ExperimentalMetricRegistry structurally refused so a plugin can never yield CERTIFIED_PRODUCT Pareto.",
  current_product_wiring="optimization/real_evaluator.py, optimization/result.py",
  classification="CANONICAL_NOW", priority="P3",
  required_action="None - record the deferral as discharged. This is a case where a documented deferral was correctly honoured rather than silently dropped."),

 dict(id="FRA-STUDIO-002", capability="Studio surfaces with no Gate-8 replacement (Debug Network, Memory, dedicated Compare)",
  domain="Studio historical surfaces",
  historical_source="p4/studio surface set vs Gate-8 wireframes",
  exact_files=["apps/studio/src/router.ts (kinds: root, project, runs, run, trust, offline, notfound)",
   "apps/studio/src/components/ (15 components)", "apps/studio/src/pages/ (design, evidence, index, offline, optimize, serving)",
   "docs/product/STUDIO-WIREFRAMES.md"],
  tests="apps/studio/tests/test_studio_contract_v2.py",
  historical_maturity="TESTED",
  current_canonical_representation="THREE HOLES. Debug Network and Memory have neither a current surface nor a Gate-8 home (MIGRATION_HOLE). A dedicated Compare surface has no home either, though comparison is reachable through Evidence (DOCUMENTED_DEFERRAL). Every other historical surface is PRESERVED or REPLACED by a stronger contract.",
  current_product_wiring="partial",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Classify the three holes into the Gate-8 IA or record them as deliberately out of scope. Do not add a surface for visual continuity alone.",
  notes="The current surfaces are BACKEND_AUTHORITY or REAL_ENGINE_CONNECTED; the historical p4 Studio was fixture-backed on some paths, so this is a strengthening, not a regression."),

 dict(id="FRA-SERVE-002", capability="Serving ownership law (which authority owns each serving control)",
  domain="serving depth / LLMServingSim scheduler",
  historical_source="LLMServingSim cluster config + serving loop",
  exact_files=["veritx_dse/simulation/serve_canonical.py::load_cluster_service_semantics (reads model_name, hardware, tp_size, pp_size, ep_size, pd_type, dp_group; deliberately does NOT read topology/link bandwidth/BookSim config)",
   "veritx_dse/simulation/serving_loop.py::CertifiedServiceProfile, build_schedulers, build_router, RequestOutcome, RoundRecord",
   "veritx_dse/simulation/llmserving_protocol.py::ServingBackendSession"],
  tests="test_serving_canonical.py, test_serving_dp.py, test_serving_ep.py, test_serving_tp_groups.py, test_serving_multiinstance_liveness.py, test_serving_liveness.py, test_serving_round.py, test_serving_scheduling_oracle.py",
  historical_maturity="SEALED",
  current_canonical_representation="ENFORCED IN CODE. The ownership boundary is stated in a source comment: reading fabric fields here 'would leak the legacy network authority into the canonical path'. Owners: SERVING EXPERIMENT (arrivals, traces, RR/LOAD, batching, prefill/decode, multi-instance, MoE dispatch/combine); CANONICAL WORKLOAD (model/cluster configs, PIM partial, CXL refused); PARALLELISM (TP, DP, PP refused, EP, instance->rank); FABRIC (explicitly not read); EVALUATION (TTFT, decode-step latency, request latency, evidence tier, provenance); BACKEND INTERNAL (queueing, rank->endpoint, execution, profiling, liveness, subprocess protocol).",
  current_product_wiring="pages/serving.tsx",
  classification="CANONICAL_NOW", priority="P3",
  required_action="Preserve. No serving control needs to be pushed into WorkloadIntent - the ones that belong there already are, and the fabric boundary is already refused at the seam."),

 dict(id="FRA-SERVE-003", capability="Serving PD (prefill/decode disaggregation) and PP (pipeline parallelism)",
  domain="serving depth / LLMServingSim scheduler",
  historical_source="LLMServingSim pd_type and pp_size",
  exact_files=["veritx_dse/simulation/serve_canonical.py::load_cluster_service_semantics (two typed ServingLoopError refusals)"],
  tests="test_serving_canonical.py",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="TYPED, REASONED, FAIL-CLOSED REFUSAL. PD: 'prefill/decode disaggregation is not supported on the canonical path (fail closed: historical PD transfer semantics have no canonical adapter yet)'. PP: 'pipeline parallelism is not supported on the canonical path (fail closed: PP SEND/RECV emission has no canonical adapter yet)'. pd_type and pp_size are still READ from the cluster config so the refusal is explicit rather than silent.",
  current_product_wiring="none - refused before execution",
  classification="NEEDS_CANONICALIZATION", priority="P2",
  required_action="Keep the refusal; it is correct. Return only with a real canonical adapter for PD transfer semantics and PP SEND/RECV emission.",
  notes="DOCUMENTED_DEFERRAL. This is the model case: the capability is refused with a named missing contract, not silently dropped."),

 dict(id="FRA-EX-002", capability="topology-family and collective example corpus as canonical regression",
  domain="historical examples",
  historical_source="tracks/t3-topology/configs/ and product/examples/",
  exact_files=["tracks/t3-topology/configs/{mesh4x4,torus4x4,cmesh16,fattree16,ftree,tree4,fly4,flatfly16,qtree16,dragonfly16,anynet16,plane_*,noc_specs}",
   "tracks/t3-topology/product/examples/{allreduce,multicast,hotspot,chiplet_2die,moe_serving}.json",
   "tracks/t3-topology/examples/*-v3.json", "tracks/t3-topology/dse/examples/*.json"],
  tests="test_full_pipeline.py, test_product_workflow.py, test_integration.py, test_serving_canonical.py",
  historical_maturity="TESTED",
  current_canonical_representation="PARTIAL. 17 BookSim topology configs exist and only mesh, torus, cmesh-concentration and AnyNet have canonical scenarios. flatfly, fly4, qtree, ftree, tree4, dragonfly and fattree are BookSim-config-only with NO canonical regression. product/examples carries allreduce, multicast, hotspot, chiplet_2die and moe_serving as real probes. GEC express / GEC-MECS / GEC mesh have neither a config nor a module.",
  current_product_wiring="product examples + presets",
  classification="NEEDS_CANONICALIZATION", priority="P1",
  required_action="Map the BookSim configs to canonical scenarios as regression probes. The fattree and GEC cases are blocked on the represent-layer reconciliation (contradiction A); the rest are not.",
  notes="Example parity requires an equivalent canonical scenario, not the survival of a primitive."),

 dict(id="FRA-MOE-001", capability="static MoE vs serving MoE separation",
  domain="serving MoE",
  historical_source="MoE serving and static evaluation paths",
  exact_files=["veritx_dse/simulation/serve_canonical.py::_is_moe, ep_size derivation",
   "tracks/t3-topology/examples/moe_8x7b_64tiles-v3.json",
   "veritx_dse/application/preset_certification.py (model_family)"],
  tests="test_serving_canonical.py, test_serving_ep.py, test_preset_certification.py",
  historical_maturity="SEALED",
  current_canonical_representation="SEPARATE. Serving has dispatch, combine, participant/rank mapping and network execution. Static product evaluation has a MoE SHAPE declaration (model_family) but NO dispatch or combine semantics. The two paths share only the fabric authority.",
  current_product_wiring="serving path; preset metadata",
  classification="CANONICAL_NOW", priority="P3",
  required_action="Keep explicit. Serving MoE capability must never be read as implying static evaluation MoE capability.",
  notes="Confirmed from source: there is no shared dispatch/combine implementation between the two paths."),

 dict(id="FRA-MEM-002", capability="PIM as a downstream-only operation kind",
  domain="PIM memory paths",
  historical_source="Wave-D graph kinds and ASTRA",
  exact_files=["veritx_dse/workload/graph.py (KIND_PIM_CHANNEL)",
   "veritx_dse/backend/astra.py (KIND_PIM_CHANNEL)"],
  tests="test_workload_graph.py, astra tests",
  historical_maturity="IMPLEMENTED",
  current_canonical_representation="DOWNSTREAM-ONLY. KIND_PIM_CHANNEL is live in the canonical workload graph AND the ASTRA backend, but no WorkloadV3 intent can originate it. Resolved against the serving question: serving does NOT use PIM through a serving-only representation, a legacy bypass, or an analytical-only path - it is simply the same canonical graph kind with no upstream declaration.",
  current_product_wiring="MEM-007 NOT_AVAILABLE / LEGACY_ONLY (inaccurate)",
  classification="NEEDS_CANONICALIZATION", priority="P0",
  required_action="Either extend intent to originate PIM, or declare the kind backend-only. MEM-007 LEGACY_ONLY is wrong: the code is live, not legacy.",
  notes="Contradiction E. Distinguished from MEM-006 REMOTE/CXL/STORAGE, which is LEGACY_ONLY and accurate because memory_lowering.py refuses them with typed reasons."),

 dict(id="FRA-META-001", capability="capability introspection and path migration classification",
  domain="capability introspection",
  historical_source="Wave-C migration ledger",
  exact_files=["veritx_dse/application/inventory.py (LEDGER, ledger_table, VALID_CLASSIFICATIONS = AUTHORITATIVE | MIGRATE | LEGACY_INTERNAL | TEST_ONLY | DELETE)"],
  tests="ledger_table() asserts every row's classification is valid",
  historical_maturity="SEALED", current_canonical_representation="CANONICAL",
  current_product_wiring="application/inventory.py",
  classification="CANONICAL_NOW", priority="P3",
  required_action="This registry EXTENDS this vocabulary rather than inventing a parallel one. inventory.py classifies PATHS; feature-reclamation-registry classifies CAPABILITIES. Keep both."),

 dict(id="FRA-META-002", capability="control-plane operation surface",
  domain="agent/API/machine-readable surfaces",
  historical_source="SrotaControlPlane",
  exact_files=["veritx_dse/application/service.py",
   "veritx_dse/application/capabilities.py (operations: capabilities, validate, compile, plan, evaluate, run_study, compare, inspect, list_results, diagnose)",
   "veritx_dse/cli/service_cli.py", "veritx_dse/api.py"],
  tests="test_application_service.py and others", historical_maturity="SEALED",
  current_canonical_representation="CANONICAL (product/service.py + gateway/app.py)",
  current_product_wiring="WIRED",
  classification="CANONICAL_NOW", priority="P3",
  required_action="Depth not audited: the historical operation list must be diffed against the current product surface to find dropped operations. Not done in this pass."),
]


REQUIRED_DOMAINS = [
 "Wave-E temporal/system-performance model",
 "clocks/time-domain semantics",
 "compute timing",
 "memory timing",
 "network timing seam",
 "deterministic scheduler",
 "resource contention",
 "bandwidth resources",
 "sensitivity/scaling",
 "analytical area",
 "power",
 "leakage",
 "timing/Fmax",
 "Timeloop",
 "energy",
 "physical/layout/link-price models",
 "Ramulator full depth",
 "PIM memory paths",
 "CXL",
 "remote memory",
 "serving depth",
 "LLMServingSim scheduler",
 "batching",
 "request arrivals/traces",
 "multi-instance",
 "serving MoE",
 "ASTRA runtime diagnostics",
 "fault injection",
 "MTU/fragmentation",
 "RTL",
 "Verilator",
 "RTL route-table generation",
 "UVM generation",
 "SVA/assertion generation",
 "RTL<->simulator validation",
 "signed manifests/export bundles",
 "comparison/comparability",
 "Wave-F candidate persistence/reopen verification",
 "Studio historical surfaces",
 "historical examples",
 "reports",
 "control-plane diagnose/list/inspect/capabilities",
 "agent/machine-readable surfaces",
]

PROTECTED_BRANCHES = [
 ("integration/canonical", "carries veritx_dse/synthesis/{milp_topology_v2,bo_synthesizer,iterative_synthesizer,event_objective}.py"),
 ("main", "carries the same synthesis module"),
 ("prod/production-readiness", "carries the same synthesis module"),
 ("rebuild/live-product-flow", "carries the same synthesis module"),
 ("validation/b4-campaign", "carries milp_topology_v2.py"),
 ("longhaul/econ-test", "carries the same synthesis module"),
 ("local/serving-preflight-guard", "carries the same synthesis module"),
 ("github/audit/wave-f-parity-ledger", "Wave-F parity evidence"),
 ("github/audit/rt-final-optimization-truth", "adversarial optimization-truth suite (14 cases)"),
 ("github/audit/rt-final-optimization-truth-v2", "candidate status/reason in records"),
 ("github/audit/rt-final-reports", "evidence-only; never merge"),
 ("github/audit/rt-final-reports-v2", "evidence-only; never merge"),
 ("github/t3-rtl-noc-backup-20260814", "RTL/NoC backup"),
 ("github/t3-rtl-noc-backup-20260815", "RTL/NoC backup"),
 ("origin/reference/gec-wire-pricing", "GEC wire-pricing variant of gec.cpp"),
 ("origin/astrasim-manal", "ASTRA-Sim topology integration"),
 ("origin/updated-booksim", "BookSim fork with local changes"),
 ("github/epic/booksim-forward-port", "FabricArtifact/MappingArtifact binding"),
]



#: Domains this pass reached with code/test evidence. A domain is verified
#: only when exact files AND tests were located, not when the topic was
#: mentioned in passing.
VERIFIED_DOMAINS = {
 "Wave-E temporal/system-performance model",
 "analytical area",
 "power",
 "leakage",
 "timing/Fmax",
 "energy",
 "physical/layout/link-price models",
 "RTL",
 "Verilator",
 "UVM generation",
 "SVA/assertion generation",
 "signed manifests/export bundles",
 "comparison/comparability",
 "Studio historical surfaces",
 "historical examples",
 "reports",
 "serving depth",
 "LLMServingSim scheduler",
 "multi-instance",
 "ASTRA runtime diagnostics",
 "MTU/fragmentation",
 "Ramulator full depth",
 "PIM memory paths",
 "clocks/time-domain semantics",
 "network timing seam",
 "deterministic scheduler",
 "sensitivity/scaling",
 "compute timing",
 "memory timing",
 "resource contention",
 "bandwidth resources",
 "request arrivals/traces",
 "batching",
 "serving MoE",
 "fault injection",
 "RTL route-table generation",
 "RTL<->simulator validation",
 "Wave-F candidate persistence/reopen verification",
 "control-plane diagnose/list/inspect/capabilities",
 "agent/machine-readable surfaces",
 "CXL",
 "remote memory",
 "Timeloop",
}

#: Reached only at surface level: the module/domain was located but the
#: required internal depth was NOT traced. Listed as partial, never counted
#: as verified.
PARTIAL_DOMAINS: set[str] = set()
_LEGACY_PARTIAL = {
 "compute timing",
 "memory timing",
 "resource contention",
 "bandwidth resources",
 "request arrivals/traces",
 "batching",
 "serving MoE",
 "fault injection",
 "RTL route-table generation",
 "RTL<->simulator validation",
 "Wave-F candidate persistence/reopen verification",
 "control-plane diagnose/list/inspect/capabilities",
 "agent/machine-readable surfaces",
}

def build() -> dict:
    return {
        "version": 1,
        "schema": "srota/feature-reclamation-registry/v1",
        "narrative": "docs/product/FEATURE-RECLAMATION-AUDIT.md",
        "generated_by": "scripts/gen_feature_reclamation.py",
        "companion_registry": {
            "path": "docs/product/capability-registry.yaml",
            "relationship": "capability-registry states what the canonical product can do NOW; this registry states what historically existed and its disposition. Neither replaces the other.",
        },
        "vocabularies": {
            "historical_maturity": ["SEALED", "TESTED", "IMPLEMENTED",
                                    "EXPERIMENTAL", "CONCEPT_ONLY", "ABSENT"],
            "classification": ["CANONICAL_NOW", "RECLAIMABLE",
                               "NEEDS_CANONICALIZATION", "RESEARCH_ONLY",
                               "GENUINELY_NEW", "REJECTED_UNSOUND"],
            "priority": {
                "P0": "required for the core product thesis before major product implementation proceeds",
                "P1": "important existing capability that must be reclaimed before product seal",
                "P2": "valuable research/diagnostic capability",
                "P3": "historical/developer-only or optional",
            },
        },
        "coverage": {
            "required_domain_count": len(REQUIRED_DOMAINS),
            "audited_domain_count": len(VERIFIED_DOMAINS),
            "partial_domain_count": len(PARTIAL_DOMAINS),
            "unaudited_domain_count": len(
                [d for d in REQUIRED_DOMAINS
                 if d not in VERIFIED_DOMAINS and d not in PARTIAL_DOMAINS]),
            "partial_domains": sorted(PARTIAL_DOMAINS),
            "unaudited_domains": sorted(
                d for d in REQUIRED_DOMAINS
                if d not in VERIFIED_DOMAINS and d not in PARTIAL_DOMAINS),
            "status": "PARTIAL",
            "reason": "Domains under not_yet_audited were NOT searched in history and are NOT classified. This registry must not be read as 'everything else is absent'.",
            "audited_domains": sorted({c["domain"] for c in CAPS}),
            "verified_domains": sorted(VERIFIED_DOMAINS),
        },
        "capabilities": CAPS,
        "branch_cleanup_rule": {
            "branches_deleted_or_modified": 0,
            "rule": "A branch may be archived ONLY after traceability exists: unique capability identified, canonical replacement identified, test replacement identified, retained commit/tag recorded. Branches holding the only implementation of a RECLAIMABLE or NEEDS_CANONICALIZATION capability MUST be retained.",
            "protected_branches": [
                {"branch": b, "why": w} for b, w in PROTECTED_BRANCHES],
            "proposed_for_archival": [],
            "archival_note": "None proposed: the audit is incomplete, so no capability has yet been shown to have a canonical + test replacement.",
        },
    }


def main(argv):
    doc = build()
    text = HEADER + "\n" + yaml.safe_dump(
        doc, sort_keys=False, allow_unicode=True, width=88, default_flow_style=False)
    if "--check" in argv:
        if not OUT.is_file() or OUT.read_text() != text:
            print("feature-reclamation-registry.yaml is STALE", file=sys.stderr)
            return 1
        print(f"registry current ({len(doc['capabilities'])} capabilities)")
        return 0
    OUT.write_text(text)
    print(f"wrote {OUT.relative_to(REPO)} ({len(doc['capabilities'])} capabilities)")
    return 0


if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))
