{
  "name": "llmservingsim",
  "description": "LLMServingSim — LLM serving traffic trace generator (KAIST)",
  "upstream": "https://github.com/casys-kaist/LLMServingSim",
  "commit": "a4053bc1161872420e1e0607cb3409ef659b828e",
  "date_vendored": "2026-09-05",
  "source_branch": "main",
  "patches": [],
  "local_modifications": [
    "serving/ kept at VeritX stack (multi-instance RR + load/run protocol, BookSim cycle-accurate backend with physical-dims, PD/DP fixes, decode trace cache, stderr-drain, --remote-memory-configuration) — see git log for third_party/llmservingsim/serving/",
    "serving/core/{block_pool,kv_cache_manager}.py + serving/{validate.sh,validate-baselines.txt} ported additively from upstream a4053bc (self-tests pass; not yet wired into scheduler)",
    "serving/core/config_builder.py: dense-DP exemption + is_moe plumbing + PP 3-dim scoping ([tp,pp,dp] dims, len-3 tp_dim/ep_dim) ported from upstream; analytical backend stays 1-dim-only (old binary) — multi-dim needs booksim",
    "serving/__main__.py: loud early-backend-death guard (exit 1 + stderr tail when 0 batches complete)",
    "inputs/, extern/ kept locally (experiment inputs, not upstream)",
    "configs/cluster dual_node_kv_remote, moe_tight_mem, rtxpro_single + configs/model Qwen3-30B-A3B-Instruct-2507 kept locally (not in upstream HEAD)",
    "astra-sim/ kept as absolute symlinks into third_party/astra-sim (see third_party/README P1); upstream .gitmodules dropped",
    "upstream .github/ dropped (their CI, not applicable to vendored copy)"
  ],
  "known_gaps": [
    "No standalone binary — runs as python -m serving",
    "Profiler requires GPU (RTXPRO6000) for hardware latency measurement",
    "validate.sh needs new upstream CLI (analytical-only, block-pool scheduler) — fails on VeritX serving base until convergence",
    "Upstream deleted the booksim network backend; veritx serve still uses our BookSim path — keep until analytical parity proven"
  ],
  "used_by": [
    "t3-topology"
  ],
  "build_command": "echo 'No build needed — Python package. Install deps: pip install -r requirements.txt'",
  "install_location": "serving/__main__.py"
}
