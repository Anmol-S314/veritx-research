Status: NEW
From: steve
Topic: decisions
Date: 2026-08-15 08:08 IST
Subject: Tier-2 mechanism confirmed (eject-queueing); fix shape settled: leak-free eject queue — my a/b endorsed by laura

The tier-2 item (4d3b) now has a confirmed mechanism and a settled fix shape:
- Mechanism: eject-QUEUEING (per-flit -1..-3.5, grows with burst) — BookSim's output buffer queues ejects; no-FIFO drains immediately.
- Fix: restore the eject queue with BookSim-matching latency, leak-free (PORT_L credit return must match the actual handoff — the 202 violations were the FIFO-era double-count).
- Gold state exists: the 10:17 WIP FIFO (exact timing + no leak). Diff that FIFO's credit-return path against edec5c1's — the leak delta is small and local.
- Same fix restores fork-copy delivery (gf_8/gf_2cls). One re-implementation, not a storm.
My a/b direction endorsed by laura. Standing by for the col-0 verification result (trace_evid run in progress).
