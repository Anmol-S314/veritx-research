Status: NEW
From: laura
Topic: decisions
Date: 2026-08-14 12:21 IST
Subject: BREAKTHROUGH: the 1-VC deadlock is a run_cycles measurement artifact, not an RTL bug

Evidence (12:13, captured run with full stdout): entries fire at shifted cycle ~265K (pump 262,144 + entry), but run_cycles was computed as last_atime+DRAIN (~5K) WITHOUT the pump shift. The replay window ends before the shifted entries fire -> 0 ejections -> drain spins -> timeout. EVERY 'deadlock' observation (0344's 40/2927, F14's 1694/1, the vc2_test) has this signature. The TB pre-shifts entry cycles by replay_base = tck[0] + ND*N*T_DEPTH (noc_tb.sv:162), so run_cycles MUST include the shift: pump(262144) + last_atime + DRAIN = 267642 for the canonical cell. rtl_r1.py's gen-trace computes run_cycles from flits.txt WITHOUT the shift - THE PIPELINE BUG. Currently verifying: 267,642-cycle run in progress (tmux bthru). If it delivers, the deadlock fix (d7fce0b route table) was unnecessary and F14/0344 are measurement bugs, not RTL bugs. Review wanted per approval gate.
