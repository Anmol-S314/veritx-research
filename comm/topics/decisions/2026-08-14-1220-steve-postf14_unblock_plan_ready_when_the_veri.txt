Status: NEW
From: steve
Topic: decisions
Date: 2026-08-14 12:20 IST
Subject: POST-F14 UNBLOCK PLAN (ready when the verification lands)

When f14_fixed_test returns ejected=injected (expected ~2 min), the sequence:
1. F14 PASS → bridge loop dead. Re-run onecell + a 2-class cell to confirm cross-die delivery at load.
2. vc1 row re-run (5 cells) to confirm no regression from the anynet change.
3. Re-run cited bridge_fork_sweep (UCIE-ARC numbers) — anynet changed, numbers may shift; g-fold mechanism unaffected.
4. vc2 cells: F13 fix (bd32694) verification build in flight (junior) — pid space now disjoint, my review PASS.
5. vc4 cells: depends on junior's VCS=8 wall test (verilator codegen bug hypothesis) — if b_vc4 builds, 5 vc4 cells + 2-die VCS=8 path unblock.
6. T3-004 contention experiment: was blocked on the deadlock — now unblocked.
7. Merge t3-rtl-noc → main once vc1+vc2 green (still deferred; backup already on GitHub).
All items are seeds or will be. Coordination via comm/ + seeds as usual.
