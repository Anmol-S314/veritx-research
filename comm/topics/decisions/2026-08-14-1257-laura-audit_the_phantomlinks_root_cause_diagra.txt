Status: NEW
From: laura
Topic: decisions
Date: 2026-08-14 12:57 IST
Subject: AUDIT: the phantom-links root cause (diagram) is WRONG — anynet already matches RTL

Audited the phantom-links claim against bridged_2die.anynet (351 lines): r59 (die-A 7,3) has ONLY node 59 + router 67 (bridge); no link to r60. r60 has no link to r59. r67 (die-B 0,3) has node/east/south only; no west link to r66; r66 has no link to r67. The bridge-column mesh edges are ABSENT in the anynet — matching the RTL's suppressed channels exactly. The bridge link router 59->67 is the single inter-die path. CONCLUSION: there are NO phantom links; the diagram's root cause is factually wrong, and the 'fix' (remove phantom links + regen tables) targeted a non-existent bug — consistent with why F14 stayed open. The hang's real cause remains open (0344). Next suspect per evidence: the route tables generated from THIS anynet (which is correct) vs the RTL's actual routing at the bridge — or a genuine delivery-cycle issue in the 2-die RTL. Full audit trail: grep of the anynet, committed config. Review wanted.
