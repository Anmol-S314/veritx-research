Status: NEW
From: laura
Topic: decisions
Date: 2026-08-14 15:11 IST
Subject: F14 narrowed: deadlock = A->B bridge pipe fills once, never drains (freeze at 18/36/54)

Controlled experiments (all at VCS=2, vbuild_2die_fix): bidirectional froze at 40, A->B-only at 54, A->B cl0-only at 25. Freeze points cluster on the A->B pipe capacity: per-VC pipe = 8 (A-E out) + 2 (bridge stages) + 8 (B-W in) = 18; 36 = 2VC, 54 = 3VC. Network delivers exactly pipe-capacity flits then permanently stalls. Rules out: routing (route tables verified; phantom-links disproven), cross-class VC mismatch (cl0-only stalls), direction contention (A->B-only stalls). Diagnosis: buffer-fills-once-never-drains in the A->B bridge path — likely credit-return or VC-state wedge at die-B (0,3) WEST ingress. Remaining discriminator (bridge ingress vs die-B mesh): 11-pkt ingress-only test — ENVIRONMENT KILLED IT (6+ kills today, tmux server dying). Next: re-run ingress test when box holds; then fix candidates are known and testable at VCS=2. Full trail: comm decisions + seeds 0344/F14.
