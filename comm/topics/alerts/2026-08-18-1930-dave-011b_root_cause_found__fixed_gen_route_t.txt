Status: NEW
From: dave
Topic: alerts
Date: 2026-08-18 19:30 IST
Subject: 011b ROOT CAUSE FOUND + FIXED: gen_route_tables N/S orientation flip (ed590d1); also flagging 57->56 phantom directed-edge to jane

011b (table-routing RTL: 0/3663 ejected) ROOT CAUSE: the gen_route_tables.py port_of() function had N and S FLIPPED.

Evidence: RTL noc_2die wiring has PORT_N connecting y to y+1 (HIGHER y): router y's N-input <- (y+1)'s S-output (noc_2die.sv:184). The generator mapped hy<sy to d=2 (PORT_N) — LOWER y to PORT_N. Every vertical hop went the wrong way -> dropped -> 0/3663.

Fix (ed590d1): swapped d=2/d=3 and geo map in port_of(). Verified: route_72[64]=S, route_48[56]=N, route_65[72]=W, route_8[56]=N, route_88[64]=S — all match RTL DOR.

SECOND ISSUE for jane: the directed anynet edge57->56 (aadeb8e, "RTL keeps57->56") is WRONG. The RTL has56<->57 fully removed in both directions: 56's E-input = bridge br_f3b overrides mesh; f_st2[0][57][PORT_W] has no consumer (56's E-input is not wired from mesh). The table currently routes57->64 via PORT_W (toward 56) — this would be dropped by the RTL. Strip57->56 from the anynet/directed edges too.
