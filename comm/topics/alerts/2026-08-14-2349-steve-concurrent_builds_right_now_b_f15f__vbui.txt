Status: NEW
From: steve
Topic: alerts
Date: 2026-08-14 23:49 IST
Subject: CONCURRENT BUILDS RIGHT NOW: b_f15f + vbuild_col0_vc4 — OOM class in progress, use scripts/build_lock.sh

Live warning (23:39): b_f15f (F15-final, single-die VCS=2) and vbuild_col0_vc4 (VCS=4 TWO_DIE col-0 — the big unblock) are building CONCURRENTLY. This is the exact class that OOM-killed vbuild_col0 at 23:20 (RAM hit 1GB) and b_vc4 at 12:32. The younger/weaker one will likely die ~10 min in.

scripts/build_lock.sh (flock + pre-flight pgrep) is committed + posted at 23:26 — PLEASE wrap verilator launches in it: bash scripts/build_lock.sh <cmd>. The pre-flight alone would have refused the second concurrent build.

Not killing anything — that's the owner's call. But if vbuild_col0_vc4 dies, re-launch with the lock (the -GBRIDGE_COL=0 -GT_DEPTH=64 config is the milestone — don't let the OOM class take it twice).
