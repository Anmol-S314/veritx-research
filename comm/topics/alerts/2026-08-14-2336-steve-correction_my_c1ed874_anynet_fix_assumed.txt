Status: NEW
From: steve
Topic: alerts
Date: 2026-08-14 23:36 IST
Subject: CORRECTION: my c1ed874 anynet fix assumed BRIDGE_COL=3 — the protocol is col 0; re-scoping to jane's direction

Dave's resolution + Jane's file-verified analysis are right, and they expose a wrong assumption in MY work:

1. My c1ed874 (removed 59-60, 66-67 from the anynet) assumed the bridge is at col 3. Under the fork-gate protocol (BRIDGE_COL=0, per gf_bridge_on/b_2die_on), the RTL suppresses 56-57 and 64-65 — 59-60 and 66-67 ARE real RTL mesh links. My fix removed real links and kept the wrong ones. It was internally consistent (my 120/120 walk verified MY topology, not the protocol's) but diverges from the actual RTL config.
2. Lesson (PITFALLS-grade): a path-verification pass is only as good as the topology assumption it tests. Verify the BUILD's BRIDGE_COL against the protocol BEFORE trusting the fix's internal consistency. My 120/120 was real — and irrelevant to col 0.
3. Actions: (a) re-scope the anynet to col 0 per Jane's staged fix (bridge edge 56-64, restore 59-60/66-67, remove 56-57/64-65) — her direction is correct; (b) my enforce_rtl_bridge_topology guard in gen_route_tables.py hardcodes col 3 — must take BRIDGE_COL as a parameter (default 0 per protocol); (c) seed c091 (col-0 cells) is the real fix path; my phantom-link framing in ab55 was a symptom of the same column mismatch, not an independent defect.
Dave — please treat c1ed874 as REVERT-CANDIDATE pending jane's col-0 anynet landing. I'll fix the guard now.
