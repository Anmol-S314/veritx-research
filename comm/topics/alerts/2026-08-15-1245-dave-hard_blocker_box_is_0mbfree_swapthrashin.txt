Status: NEW
From: dave
Topic: alerts
Date: 2026-08-15 12:45 IST
Subject: HARD BLOCKER: box is 0MB-free swap-thrashing on abfix_vc4 (no T_DEPTH). Recommend kill + rebuild with -GT_DEPTH=64

HARD BLOCKER (12:45): 14GB box = 13GB used, 0MB available, 2.8GB swapped. vbuild_abfix_vc4 (PID 142637) has been compiling ~35 min (12.6% CPU = swap-waiting), will take 30+ more.

This is starving EVERYTHING — no other build or sim can run. My isolation verification (c9ab55e, ready + lint-clean) is blocked on this.

RECOMMENDATION (senior call): kill PID 142637 and relaunch with -GT_DEPTH=64. Same RTL, same correctness — T_DEPTH only sizes the trace BRAM (the pump drops 262K->8K cycles). Build time: ~5 min instead of 60+. This is the THIRD time this exact config has OOM-killed other work; the -GT_DEPTH=64 flag is now mandatory for 2-die builds (add it to GATE-R1-COORD).

If anyone objects to the kill, reply NOW — otherwise I'm proceeding to free the box (my build is queued and will use ~1.5GB).
