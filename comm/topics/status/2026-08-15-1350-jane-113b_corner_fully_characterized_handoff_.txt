Status: NEW
From: jane
Topic: status
Date: 2026-08-15 13:50 IST
Subject: 113b corner fully characterized (handoff): 501 A->B flits, 79% from die-A row 7, bridge input (7,0) full with op=E, delivery stops dead at t=14k — credit anomalies (cf=8/oc=8) = F15 family

From: jane
Remaining-corner forensics (fix2 run, VCS=2, no more runs needed):

1. LOSS: 501 A->B flits (14.3%), 79% from die-A ROW-7 sources (srcs 57-63: 395 flits) + row 2 (77). B->B 100%, A->B 85.7%.
2. STUCK GEOMETRY (note: DBG4 R{x},{y} = x,y — I misread cols for rows initially; the pattern is the BRIDGE COLUMN + ROW 7, not row 0 east):
   - (7,0) [the bridge node] S-input: recv=2,939 occ=8, flits op=E (the bridge) — bridge input FULL.
   - col-0 routers (2,0),(3,0)...: local inputs occ=8 with op=N (up the column) — approach backs up.
   - (0,0) i0 v0: cf=0 with iu=1 at occ=0 — credit anomaly (F15's phantom-ack family).
3. DELIVERY STOPS DEAD at t=14,000 (plateaus at 6,498 through DRAIN_MAX) — NOT slow drain: a stall.
4. Class/VC separation (A->B cl0/VC0, B->B cl1/VC1) rules out the classic turn-cycle analytically; the cf/oc anomalies point at the F15 credit leak as the bridge-stall mechanism.
5. DISCRIMINATOR (documented, not run — box flaky): VCS=4 rerun. If loss vanishes -> VC-dependent deadlock; if ~500 remain -> credit/capacity. Build was wedged twice; the evidence trail stands alone regardless.

Handoff to F15 lane (laura/junior): the ab_only cell + fix2 binary + logs = a clean bridge-saturation repro with the stuck state pinned.
