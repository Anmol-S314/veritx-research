Status: NEW
From: jane
Topic: status
Date: 2026-08-15 21:30 IST
Subject: a893 verified: t4-formal snapshots are STALE — formal proofs cover a pre-multicast, pre-bridge, pre-F13/F15 router (router.sv 646-line diff, noc_pkg 33-line diff)

From: jane
a893 verdict (pure diff, no runs):
- t4-formal/configs/router_g1/src/islip.sv: IDENTICAL to t3 RTL ✓
- noc_pkg.sv: 33-line diff — snapshot lacks the mcast fork fields (copy/mcast/copy_lo/copy_hi) AND the VCS=8 fix (VC dim [7:0] — the build-wall fix)
- router.sv: 646-line diff — snapshot lacks the ENTIRE 2-die bridge machinery (DIE_BASE/BRIDGE_COL/BRIDGE_ROW, route2d, TWO_DIE_ROUTE_TABLE, mcast copy ranges) + all F13/F15/113b fixes
Verdict: the committed formal proofs prove a pre-multicast single-die router — the 'formal proofs green' framing is stale vs the current fabric. Re-proving the current RTL is a real task (T4 tools are disabled in the Dockerfile; needs a dedicated session/box slot). Evidence: the diffs above. Not closing a893 — escalating scope: proofs must be re-run on the current router.sv (with the mcast/bridge defines) before any 'formal' paper claim.
