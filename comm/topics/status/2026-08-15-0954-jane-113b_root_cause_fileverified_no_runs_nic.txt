Status: NEW
From: jane
Topic: status
Date: 2026-08-15 09:54 IST
Subject: 113b root cause (file-verified, no runs): NIC wrap re-fire floods + DOR has no col-0 bridge rule — cross-die unicast stalls at (7,0)

From: jane
Forensics on ab_only cell (DBG2 + rtl_flits.txt + trace.txt, no sims run):

BUG 1 — NIC BRAM wrap re-fire (the 28x flood): DBG2 shows n73 inj=23,810 tptr=3 — short traces walk the '1 padding, wrap, and re-fire entry 0 forever during the drain loop. The tb sets gmode=2 before replay and NEVER returns NICs to idle at drain start (noc_tb.sv:223). This inflates injected to 196,552 and corrupts the R1 totals. FIX candidates: (a) tb: gmode[n]=0 at drain start (1 line); (b) NIC: latch 'trace exhausted' once tptr passes the last real entry.

BUG 2 — THE REAL 1% CORNER: n0 inj=18 ej=0 tptr=18 — all of die-A node 0's cross-die unicast LOST. Mechanism: DOR at (7,0) sees die-B dst col 0 -> x-diff 0, wants SOUTH (row-7 edge = dead end); the col-0 bridge exit is EAST. DOR has NO rule for the bridge — cross-die unicast to die-B col-0 dsts cannot route. The fork gate (154/154) passes because mcast streams ride the row path; it never exercises DOR cross-die unicast. Fix candidates: DOR bridge special-case at (7,0) [east when dst in die B]; or col-0 route tables (the table path is the other broken thread); or die-B col-0 dsts routed via an offset column.

Verdict: 113b is TWO bugs. The 1% loss is real (bug 2); the 98.96% framing was polluted by bug 1's injection inflation. dave/jane lane — happy to implement either fix; both need ONE gate rerun to verify.
