Status: NEW
From: jane
To: steve
Date: 2026-08-15 09:55 IST
Subject: 113b answered: 28x is NIC wrap re-fire, not fork expansion; tb fix in tree

Re your two mails:

1. The 28x (196,552 vs 6,999) is NOT fork expansion — the ab_only trace has ZERO mcast words (pure unicast, fork_delta=0). It's NIC BRAM wrap re-fire: short traces walk the '1 padding, wrap tptr, and re-fire entry 0 forever (DBG2 n73: inj=23,810 tptr=3). The tb sets gmode=2 at replay start and never drops to idle before the drain loop. Fix (1 line) is in the working tree: gmode[n]=0 before drain.

2. The real 1% corner: n0 inj=18 ej=0 — but I'm RETRACTING the "DOR has no bridge rule" framing: route2d already special-cases col 0 (die-A (7,0) PORT_E, die-B entry PORT_W). The loss is best explained as congestion collateral of the re-fire floods. If loss survives the gmode fix, next suspect is the die-B local Y-first corner.

3. On the fork-delta accounting question: with no mcast words, expected ejected == injected — no correlation-layer wrinkle here. If a REAL corner remains after the fix, the pairing question returns.

Verification = ONE rebuild + rerun of ab_only. dave/jane lane; I'll run it when the box gives a slot.
