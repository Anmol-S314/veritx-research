Status: NEW
From: steve
To: laura
Date: 2026-08-14 22:58 IST
Subject: F15 leak mechanism: F8 (filed) — the bypass + unconditional eject credit = +1 drift per fork copy

Connecting Junior's empirical finding to the filed audit finding — I think F15's credit leak IS F8 (T3-003/9bba, from the RTL audit), and I can point at the exact lines:

- router.sv:394-395: fork copy marked `vc = '0; // copy marker: no credit return`
- nic.sv:188-189: `eject_credit.valid = eject.valid` — the NIC returns a credit for EVERY eject, unconditionally, marker or not
- The copy bypasses the input buffer (goes straight to XT/PORT_L) → no decrement when sent → the returned credit is a pure +1 drift at credit_free[PORT_L][vc]. Junior's DBG5 audit (202 phantom-ack violations on fork-capable trees, ZERO on the ladder, scales with mcast: gf_8 leaks 8, gf_2cls 379) fits exactly.

Your micro-bisect will confirm (disable the PORT_L delivery override → if 44,274 delivers, the bypass/FIFO block is the source). Fix directions, in order of cleanliness:
(a) NIC: suppress eject_credit for fork copies — but the vc=0 marker collides with real vc-0 traffic, so the marker must be made unambiguous (e.g., a dedicated copy bit on the link, or the NIC keys on a field real packets never set);
(b) Router: don't count the NIC's PORT_L credit return for copy-marked flits (keep the marker on the link);
(c) No bypass: make the copy occupy the local input buffer like a real packet (credit accounting becomes consistent by construction, at one cycle of latency).
Direction (c) is the most structurally sound — it eliminates the phantom-credit class instead of patching the counter.

The F8 seed (T3-003/9bba) predicted this; happy to close it with your evidence once the bisect lands.
