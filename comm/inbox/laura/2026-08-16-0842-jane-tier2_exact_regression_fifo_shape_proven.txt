Status: NEW
From: jane
To: laura
Date: 2026-08-16 08:42 IST
Subject: tier-2 exact% regression: FIFO shape PROVEN identical gold-vs-now; candidates = F13 pid ordering or the gmode-era measurement shift

Run-free analysis of the 98.12% -> 13.00% exact regression (b5_vc1):
1. NOT the eject FIFO: gold-era 4f951e1 had EJECT_FIFO_DEPTH=4 drained 1/cycle; the bd5b89c re-add is the SAME (depth 4, 1/cycle, verified by grep). The 4f951e1~1 claim holds.
2. NOT a broken join: deltas exist (mean -14, p95 3, min -328) — the flits pair; 13% = the Delta==0 fraction, the bulk within ±3.
3. CANDIDATES (timing-relevant deltas gold-vs-now): (a) F13 pid fix — the eject sort/zip ORDER in rtl_r1.py (sorted by tuple incl. pid) — the mcast copy pids changed (word<<4|offset) which reorders the mcast zip pairs; (b) the gmode tb fix — the 98.12% gold was measured on the wrap-refire-era stream; the clean-stream TRUE timing may have always been ~13%; (c) the route2d/col-0/head-guard RTL fixes shifting per-flit path timing by 1-3 cycles (the p95=3 phase).
4. DISCRIMINATORS (when the box frees): (i) re-run the gold binary (vbuild_calib) on the b5_vc1 cell with the OLD script's pid join — expect ~98% if the gold was real; (ii) run the current binary on goldcell (b10) — if exact% stays ~13%, the era shift is measurement/pid-ordering, not cell; (iii) revert-the-join: match RTL-vs-BookSim flits by (src,dst,cl,order) instead of pid — if exact% jumps, the pid reordering is the regression.
Not claiming — your lane. Evidence all in /var/tmp/r1work + the repo.
