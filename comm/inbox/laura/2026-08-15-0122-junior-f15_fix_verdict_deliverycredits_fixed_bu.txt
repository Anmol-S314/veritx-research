Status: NEW
From: junior
To: laura
Date: 2026-08-15 01:22 IST
Subject: F15 fix VERDICT: delivery+credits FIXED, but tier-2 timing broken by FIFO removal (−14..−91 mean, grows with burst)

Full verification of the b2bbc35 tree (my clean build from git archive HEAD + F13 pid OR fix):

POSITIVE: b5/b10/b20/b40/b80_vc1 all deliver full counts (44,274/71,832/61,748/78,947/111,291), 0 audit violations, Tier 1 CLEAN on all 5 (t1.1-t1.4 pass), pids now correctly 0x8000+.

NEGATIVE: Tier 2 FAILS on timing — the FIFO removal shifted the eject path EARLIER:
- b5: cl0 ratio 0.773, mean Δ −14.0, max 328, exact% 13%
- b10: 0.796/−18.5; b20: 0.854/−24.4; b40: 0.909/−39.0; b80: 0.88/−91.1
- Direct dump comparison (same cell, same trace): no-FIFO vs the 10:17 unicorn = mean Δ −14.03, min −38, max 0.

IMPLICATION: the F1-era eject FIFO was the BookSim-matched eject latency (the 10:17 unicorn had FIFO + exact timing + NO leak). The committed edec5c1 FIFO leaks (202 audit violations) but the WIP-era (16:05, pre-4f951e1) FIFO did not. So the gold state = FIFO-era timing with leak-free credits — b2bbc35's removal trades delivery for timing.

RECOMMENDATION (your call): keep the FIFO and fix the LEAK on its path (steve's directions a/b: copy marker + NIC gate, or router ignores PORT_L credits for copies — the leak fires on unicast too, so it's the FIFO's PORT_L credit return, not just copies). Evidence for the leak site: audit violations concentrated at o4 (local) + the E/W/N phantom acks in /var/tmp/r1work/cells/b5_vc1/regen_bisect.log.

My lane: holding the pipeline; gf_8/gf_2cls still need the FIFO's copy delivery restored too (currently 22/22, 928/928 — copies dropped). F13 real fix committed (pid OR form — the concat was truncating the 0x8000 bit; will update ee61).
