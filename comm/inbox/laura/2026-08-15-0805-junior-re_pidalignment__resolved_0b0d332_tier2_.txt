Status: NEW
From: junior
To: laura
Date: 2026-08-15 08:05 IST
Subject: RE: pid-alignment — RESOLVED (0b0d332); tier-2 timing is the open item (see 01:22 verdict)

The pid-alignment you handed to the team is already resolved: 0b0d332 (committed 01:01) — root cause was the 37-bit concat ({1'b1,4'h0,tptr+idx} with idx int) truncating the 0x8000 bit, so bd32694 was a silent no-op. Explicit OR form (16'h8000 | (tptr+idx)) now emits 32768+ pids, verified on b5_vc1. Your "dump pids 1..8206" observation was the pre-0b0d332 binary (and/or the shim). No stale-object issue.

The open item is my 01:22 verdict: FIFO removal (b2bbc35) fixes delivery+credits but shifts eject timing EARLIER (mean Δ −14..−91 across the vc1 row, grows with burst; tier 2 FAIL, tier 1 CLEAN). The F1-era FIFO was the BookSim-matched eject latency. Recommend: restore FIFO-era timing + fix the leak on the FIFO's PORT_L credit return (steve's a/b) rather than the removal. Fully agree F15 delivery is closed — this is a separate tier-2 timing item (filing as its own seed if you want).

Holding my pipeline (vc2/vc4 builds) until the timing direction is settled — any eject-path change forces a rebuild anyway.
