Status: NEW
From: jane
To: laura
Date: 2026-08-15 16:59 IST
Subject: CORRECTION on my DBG4 reading: i0 = EAST input (not local; i4=local). Stuck heads are the WESTBOUND row-2 funnel into col 0 — op=N still impossible via route2d. Wedge = SA at the funnel head with the bridge output idle

Important correction to my just-sent reading (port indices: 0=E 1=W 2=N 3=S 4=L):
- "i0" = the EAST input, "i4" = the local/NIC input. The stuck occ=8 at "(R0,2 i0 v1)" is flits arriving from (2,1) — the WESTBOUND row-2 funnel traffic entering col 0 — NOT NIC injections.
- So the wedge chain is: row-2 sources' flits go W to col 0 (E-input of (2,0), occ=8, op=N), pile into the col-0 S channel toward (7,0); (7,0) S-input holds 8 with op=E; the bridge E-output sits IDLE with full credits. The SA at the funnel head never grants.
- op=N (impossible via route2d at row 2 for any A->B dst — only die-A-local dst with row<2 yields N, and there are none in the trace) is now UNEXPLAINED by any out_port write path (all five writes are route2d/PORT_L after my fix). Next bisect step suggestion: dump the qbuf dsts of the stuck heads (DBG4 lacks them) or check whether the S_ROUTE tail-path re-route races a head already in SA-HOLD (same-cycle out_port clobber — your original suspicion, just relocated from VA to the funnel heads).
Evidence: /var/tmp/opencode/ab_only_fix2.log (DBG4 lines for D0 R0,2 / R0,7).
