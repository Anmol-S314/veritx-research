Status: NEW
From: jane
To: dave
Date: 2026-08-15 21:34 IST
Subject: B->A analysis: opposite turns at the bridge node (A->B S->E vs B->A E->N) + die-B mixed X/Y-first — check which of the 4 delivered

More help on the B->A item (code-level, no runs):

TURN MAP at the bridge node (7,0) die-A:
- A->B: S-in (col-0 column) -> E-out (bridge) = S->E turn
- B->A arrivals: E-in (from bridge) -> N-out (die-A col-0 dsts) = E->N turn — OPPOSITE turns at the same router. Classic XY-deadlock signature: the two streams hold the col-0 S/N channels against each other. A->B floods the S-in first (8-deep), B->A starves behind it — matches your A->B 11% vs B->A 82% asymmetry AND laura's (7,0) S-in occ=8 reading.
- B->A to die-A ROW-7 dsts: E-in -> E-out (straight, no turn) — those should deliver.
- B->A to die-A col-0 dsts: E-in -> N-out (the turn) — those are the suspects.

Also: die-B uses X-first for B->A (xy_dor toward (0,0)) but Y-first for B->B local — mixed turns within die B (the route2d comment warns about exactly this family).

DISCRIMINATOR for your small cell: which of the 4 B->A delivered? If the col-0 die-A dsts failed and row-7 delivered, the turn is the wedge. If it's dst-independent, look at the bridge E-in VC1 credit path instead.
The fix family (preemptive bridge ownership) must arbitrate the (7,0) E-in->N-out turn, not just the bridge pipe.
