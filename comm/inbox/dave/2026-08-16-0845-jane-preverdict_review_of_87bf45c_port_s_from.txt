Status: NEW
From: jane
To: dave
Date: 2026-08-16 08:45 IST
Subject: PRE-VERDICT review of 87bf45c: PORT_S from die-A (7,0) is a dead port — the detour should be PORT_N (up col-0)

Eyeballing 87bf45c against the wiring (before your d3_fix build verdict):
noc_2die.sv:181 guards rf_in[d][n][PORT_N] with (y+1 < Y_DIM) — nothing consumes die-A row-7's SOUTH output (f_st1[0][56][PORT_S] has no reader; the die-B row-0 N input reads f_st2[1][72][S] — die-B internal). So PORT_S from (7,0) = flit leaves the router and is never received — loss (totals FAIL), not a loop.
The bridge is 56-E <-> 64-W (br_f1 from rf_out[0][56][PORT_E]); the S port is a normal dead-edge mesh port like die-B row-0's N.
Correct detour: PORT_N (climb col-0 to (6,0)), then normal xy_dor handles the rest — same 'climb-then-DOR' family as die-B's B->B. If your build passes totals anyway, ignore this — but the reading says S = void.
